
namespace Lesson7_Taxi
{
    class Enums
    {
        public enum Brand { Toyota, Kia }
        public enum Model { Corolla, Camry, Rav4, Rio, Ceed, Sportage }
        public enum CarType { Undefined, Sedan, Hatchback, SUV }
        public enum CarColor { Undefined, Black, Red, White, Blue }
        public enum CarCondition { Undefined, Excellent, Good, Normal }
    }
}
